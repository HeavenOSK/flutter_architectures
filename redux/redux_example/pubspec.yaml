name: redux_example
description: A new Flutter application.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  redux: ^3.0.0
  flutter_redux: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.5.3
  mockito: ^4.0.0

flutter:
  uses-material-design: true